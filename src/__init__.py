"""Top-level package for device-advisor.

Currently just `scraper` (collects device data from GSMArena) and `shared`
(cross-cutting config, logging, and storage helpers). See each subpackage's
docstring for details.
"""
